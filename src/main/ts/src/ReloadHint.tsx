import React from 'react'

export default function ReloadHint() {
  return (<small><br/>
    Please note that there is not need to reload this browser windows, 
    as we're polling the server to recognize whether you've updated your
    &nbsp;<code>AnimationStrategy</code>. 
  </small>)
}
